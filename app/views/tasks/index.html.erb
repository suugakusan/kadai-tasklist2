<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to "タスク", task %> : <%= task.status %> &gt;  <%= task.content %></li>
  <% end %>
</ul>

<%== pagy_nav(@pagy) %>

<%= link_to '新規タスクの投稿', new_task_path %>